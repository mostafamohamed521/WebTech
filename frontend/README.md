# WEBTECH Frontend — Git Commit Log

Chronological log of every file created in this part of the project, with the exact git command used for each:

```bash
git add frontend/package.json
git commit -m "package.json: اعتمادات الفرونت اند (React 19, Three.js, GSAP, Framer Motion...)"
```

```bash
git add frontend/vite.config.ts
git commit -m "vite.config.ts: إعدادات Vite ومسار الـ alias @/"
```

```bash
git add frontend/tailwind.config.js
git commit -m "tailwind.config.js: نظام الألوان والخطوط الفاخر لـ WEBTECH"
```

```bash
git add frontend/postcss.config.js
git commit -m "postcss.config.js: إعدادات PostCSS لـ Tailwind"
```

```bash
git add frontend/tsconfig.json
git commit -m "tsconfig.json: إعدادات TypeScript"
```

```bash
git add frontend/index.html
git commit -m "index.html: صفحة HTML الرئيسية"
```

```bash
git add frontend/src/main.tsx
git commit -m "src/main.tsx: نقطة دخول تطبيق React"
```

```bash
git add frontend/src/styles/globals.css
git commit -m "src/styles/globals.css: الأنماط العامة (Tailwind + متغيرات التصميم)"
```

```bash
git add frontend/src/App.tsx
git commit -m "src/App.tsx: تركيب التطبيق الرئيسي (Providers + Router)"
```

```bash
git add frontend/src/routes/AppRoutes.tsx
git commit -m "src/routes/AppRoutes.tsx: تعريف مسارات الصفحات"
```

```bash
git add frontend/src/pages/Home/HomePage.tsx
git commit -m "src/pages/Home/HomePage.tsx: صفحة الهوم واستدعاء الأقسام"
```

```bash
git add frontend/src/components/home/HeroSection.tsx
git commit -m "src/components/home/HeroSection.tsx: قسم الهيرو الرئيسي بالـ 3D"
```

```bash
git add frontend/src/pages/Errors/NotFoundPage.tsx
git commit -m "src/pages/Errors/NotFoundPage.tsx: صفحة خطأ 404"
```

```bash
git add frontend/src/services/apiClient.ts
git commit -m "src/services/apiClient.ts: عميل Axios الأساسي والتعامل مع التوكن"
```

```bash
git add frontend/src/store/authStore.ts
git commit -m "src/store/authStore.ts: حالة المصادقة (Zustand)"
```

```bash
git add frontend/src/store/cartStore.ts
git commit -m "src/store/cartStore.ts: حالة سلة التسوق (Zustand)"
```

```bash
git add frontend/src/types/product.ts
git commit -m "src/types/product.ts: أنواع TypeScript الخاصة بالمنتج"
```

```bash
git add frontend/src/hooks/useProducts.ts
git commit -m "src/hooks/useProducts.ts: هوك جلب قائمة المنتجات (React Query)"
```

```bash
git add frontend/.env.example
git commit -m ".env.example: نموذج متغيرات البيئة للفرونت اند"
```

```bash
git add frontend/.gitignore
git commit -m ".gitignore: تجاهل ملفات الفرونت اند المؤقتة"
```

```bash
git add frontend/src/components/layout/.gitkeep
git commit -m "src/components/layout/.gitkeep: حجز فولدر src/components/layout في هيكل المشروع"
```

```bash
git add frontend/src/components/product/.gitkeep
git commit -m "src/components/product/.gitkeep: حجز فولدر src/components/product في هيكل المشروع"
```

```bash
git add frontend/src/components/cart/.gitkeep
git commit -m "src/components/cart/.gitkeep: حجز فولدر src/components/cart في هيكل المشروع"
```

```bash
git add frontend/src/components/ui/.gitkeep
git commit -m "src/components/ui/.gitkeep: حجز فولدر src/components/ui في هيكل المشروع"
```

```bash
git add frontend/src/pages/Product/.gitkeep
git commit -m "src/pages/Product/.gitkeep: حجز فولدر src/pages/Product في هيكل المشروع"
```

```bash
git add frontend/src/pages/Cart/.gitkeep
git commit -m "src/pages/Cart/.gitkeep: حجز فولدر src/pages/Cart في هيكل المشروع"
```

```bash
git add frontend/src/pages/Checkout/.gitkeep
git commit -m "src/pages/Checkout/.gitkeep: حجز فولدر src/pages/Checkout في هيكل المشروع"
```

```bash
git add frontend/src/pages/Account/.gitkeep
git commit -m "src/pages/Account/.gitkeep: حجز فولدر src/pages/Account في هيكل المشروع"
```

```bash
git add frontend/src/pages/Category/.gitkeep
git commit -m "src/pages/Category/.gitkeep: حجز فولدر src/pages/Category في هيكل المشروع"
```

```bash
git add frontend/src/pages/Search/.gitkeep
git commit -m "src/pages/Search/.gitkeep: حجز فولدر src/pages/Search في هيكل المشروع"
```

```bash
git add frontend/src/pages/Compare/.gitkeep
git commit -m "src/pages/Compare/.gitkeep: حجز فولدر src/pages/Compare في هيكل المشروع"
```

```bash
git add frontend/src/pages/Wishlist/.gitkeep
git commit -m "src/pages/Wishlist/.gitkeep: حجز فولدر src/pages/Wishlist في هيكل المشروع"
```

```bash
git add frontend/src/pages/Support/.gitkeep
git commit -m "src/pages/Support/.gitkeep: حجز فولدر src/pages/Support في هيكل المشروع"
```

```bash
git add frontend/src/three/scenes/.gitkeep
git commit -m "src/three/scenes/.gitkeep: حجز فولدر src/three/scenes في هيكل المشروع"
```

```bash
git add frontend/src/three/models/.gitkeep
git commit -m "src/three/models/.gitkeep: حجز فولدر src/three/models في هيكل المشروع"
```

```bash
git add frontend/src/utils/.gitkeep
git commit -m "src/utils/.gitkeep: حجز فولدر src/utils في هيكل المشروع"
```

```bash
git add frontend/src/assets/icons/.gitkeep
git commit -m "src/assets/icons/.gitkeep: حجز فولدر src/assets/icons في هيكل المشروع"
```

```bash
git add frontend/src/assets/images/.gitkeep
git commit -m "src/assets/images/.gitkeep: حجز فولدر src/assets/images في هيكل المشروع"
```
